# Selected extras for this project (air-gapped server + GUI).
# Included when INCLUDE_RECOMMENDED=yes in config.env.

# --- Admin / observability (user-selected) ---
# htop / nload: NOT in RHEL BaseOS/AppStream — listed in packages/epel-extra.txt
iotop
sysstat
lsof
strace
bash-completion
man-pages
man-db

# --- Desktop extras (user-selected) ---
firefox
evince
gnome-terminal
gnome-system-monitor

# --- Media (RHEL AppStream / CRB) ---
# ffmpeg + some GStreamer codecs are NOT in RHEL CDN — see packages/epel-extra.txt
gstreamer1-plugins-base
gstreamer1-plugins-good
gstreamer1-plugins-good-gtk
# free "bad" plugins when available on AppStream/CRB (name may be -free)
gstreamer1-plugins-bad-free

# --- Automation (git is also in required.txt with gitk/git-lfs) ---
# no gcc/make toolchain by default

# --- Small CLI helpers commonly needed offline ---
psmisc
procps-ng
which
file
less
diffutils
